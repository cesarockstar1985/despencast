<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DespenCast | Panel de Control</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; background-color: #f4f7f6; }
        .sidebar { background: #1a1a2e; color: white; min-height: 100vh; }
        .card-stats { border: none; border-radius: 12px; transition: transform 0.2s; }
        .card-stats:hover { transform: translateY(-5px); }
        .table-container { background: white; border-radius: 12px; padding: 20px; box-shadow: 0 4px 6px rgba(0,0,0,0.05); }
        .status-badge { font-size: 0.8rem; padding: 5px 10px; border-radius: 20px; }
        .text-primary-custom { color: #4e73df; }
    </style>
</head>
<body>

<div class="container-fluid">
    <div class="row">
        <div class="col-md-2 d-none d-md-block sidebar p-4">
            <h4 class="mb-4">DespenCast</h4>
            <nav class="nav flex-column">
                <a class="nav-link text-white active" href="/dashboard">🏠 Resumen</a>
                <a class="nav-link text-white-50" href="/clientes">👥 Clientes</a>
                <a class="nav-link text-white-50" href="#">💰 Pagos</a>
            </nav>
        </div>

        <main class="col-md-10 p-4">
            <header class="d-flex justify-content-between align-items-center mb-5">
                <div>
                    <h2 class="fw-bold">Panel de Gestión</h2>
                    <p class="text-muted">Estado actual de la despensa en tiempo real</p>
                </div>
                <button class="btn btn-primary shadow-sm" onclick="location.reload()">🔄 Actualizar Datos</button>
            </header>

            <div class="row mb-4">
                <div class="col-md-4">
                    <div class="card card-stats shadow-sm p-3">
                        <div class="text-muted small fw-bold">TOTAL POR COBRAR</div>
                        <div class="h3 fw-bold text-danger mt-2">
                            Gs. <%= totalDeuda.toLocaleString('es-PY') %>
                        </div>
                        <div class="text-muted extra-small">Cuentas pendientes (Fiar)</div>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="card card-stats shadow-sm p-3">
                        <div class="text-muted small fw-bold">PEDIDOS ACTIVOS</div>
                        <div class="h3 fw-bold text-primary-custom mt-2"><%= pedidos.length %></div>
                        <div class="text-muted extra-small">Registros sin marcar como pagados</div>
                    </div>
                </div>
            </div>

            <div class="table-container shadow-sm">
                <h5 class="fw-bold mb-4 text-dark">Detalle de Deudores</h5>
                <div class="table-responsive">
                    <table class="table table-hover align-middle">
                        <thead class="table-light">
                            <tr>
                                <th>Fecha</th>
                                <th>Cliente (ID)</th>
                                <th>Producto</th>
                                <th class="text-end">Monto</th>
                                <th class="text-center">Estado</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (pedidos.length > 0) { %>
                                <% pedidos.forEach(p => { %>
                                <tr>
                                    <td class="text-muted small"><%= p.fecha_pedido.split('T')[0] %></td>
                                    <td>
                                        <div class="fw-bold text-dark">Usuario #<%= p.cliente_id %></div>
                                    </td>
                                    <td><%= p.nombre_producto %></td>
                                    <td class="text-end fw-bold">Gs. <%= p.precio_pedido.toLocaleString('es-PY') %></td>
                                    <td class="text-center">
                                        <span class="status-badge bg-warning text-dark fw-bold">PENDIENTE</span>
                                    </td>
                                </tr>
                                <% }) %>
                            <% } else { %>
                                <tr>
                                    <td colspan="5" class="text-center p-5 text-muted">
                                        ✨ ¡No hay deudas pendientes! Todo está al día.
                                    </td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </main>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>